export default function Footer() {
  return (
    <footer className="mt-10 text-xs opacity-60 flex flex-wrap gap-4">
      <span>Blockscout: tx deep links</span>
      <span>Avail: Bridge & Execute (intent UX)</span>
      <span>Envio: live leaderboard (planned)</span>
    </footer>
  );
}
